engines:
  duplication:
    enabled: true
    config:
      languages:
        php:
          mass_threshold: 100
  fixme:
    enabled: true
  phpmd:
    enabled: false
ratings:
  paths:
  - "**.php"
exclude_paths:
  - tests/